"""CLI entry point for the isnad-graph platform."""

from __future__ import annotations

import sys


def main() -> None:
    """Run the isnad-graph CLI."""
    print("isnad-graph CLI — not yet implemented")
    sys.exit(0)


if __name__ == "__main__":
    main()
